/**
 * __t — runtime translation helper used inside compiled render functions.
 * Falls back to the key if no translation is found.
 */
export declare function __t(key: string, locale?: Record<string, string>): string;
export declare function loadLocale(locale?: string, cwd?: string): Record<string, string>;
//# sourceMappingURL=translate.d.ts.map