import { defineEvent } from '@lumiarq/framework';
import { z }           from 'zod';

export const PasswordReset = defineEvent({
  name:   'iam.password-reset',
  schema: z.object({
    identityId:     z.string().uuid(),
    resetToken:     z.string(),
  }),
});
